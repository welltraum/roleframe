# Fixture audits

These audit files are stable fixtures for dashboard smoke evals.

They are intentionally small but contain:

- score summaries
- criteria tables
- top deficits
- backlog entries with file references

Use them when testing `/roleframe dashboard` without depending on a live project or a fresh `review -> dashboard` run.
