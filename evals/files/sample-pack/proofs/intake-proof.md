# Intake proof

The pack claims explicit-only invocation and hidden visibility.

Observed evidence:

- manifest declares `invocation=explicit-only`
- route file exists

Missing evidence:

- no typed payload example
- no rollout check proving hidden route exposure stays blocked
