# Contract checks

- Verify that the route exists in the manifest.
- Verify that hidden routes are not exposed by default.
- Verify that donor intake uses explicit invocation.

Missing on purpose:

- no literal payload schema
- no check that `visible` and `owned_surfaces` stay aligned
